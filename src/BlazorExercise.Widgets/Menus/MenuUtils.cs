namespace BlazorExercise.Widgets.Menus;

internal static class MenuUtils
{
    public static class PropertyNames
    {
        public const string MenuLevel = $"${nameof(Menu)}::${nameof(MenuLevel)}";
    }
}